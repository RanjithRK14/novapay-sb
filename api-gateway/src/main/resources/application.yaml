server:
  port: ${SERVER_PORT:10000}

spring:
  application:
    name: api-gateway-service

  data:
    redis:
      url: ${REDIS_URL:rediss://default:gQAAAAAAAVTkAAIncDI5OTFiMDFkNjRmMTc0MDUxYjBmOTc1ZDYwOTFhM2U4YnAyODcyNjg@dominant-reindeer-87268.upstash.io:6379}
      connect-timeout: 2s
      timeout: 1s

  cloud:
    gateway:
      httpclient:
        connect-timeout: 10000
        response-timeout: 90s
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns:
              - ${FRONTEND_URL:https://novapay-app.netlify.app}
            allowedMethods: [GET, POST, PUT, DELETE, OPTIONS, PATCH, HEAD]
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

      routes:
        - id: user-service-auth
          uri: ${USER_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/auth/**

        - id: user-service-api
          uri: ${USER_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/users/**

        - id: wallet-service
          uri: ${WALLET_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/v1/wallets/**

        - id: transaction-service
          uri: ${TRANSACTION_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/transactions/**

        - id: reward-service
          uri: ${REWARD_SERVICE_URL:http://localhost:8085}
          predicates:
            - Path=/api/rewards/**

        # NOTE: controller is @RequestMapping("/api/notify") — NOT /api/notifications
        - id: notification-service
          uri: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/notify/**

        - id: user-service-ping
          uri: ${USER_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/ping/user
          filters:
            - RewritePath=/ping/user, /ping

        - id: wallet-service-ping
          uri: ${WALLET_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/ping/wallet
          filters:
            - RewritePath=/ping/wallet, /ping

        - id: transaction-service-ping
          uri: ${TRANSACTION_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/ping/transaction
          filters:
            - RewritePath=/ping/transaction, /ping

        - id: reward-service-ping
          uri: ${REWARD_SERVICE_URL:http://localhost:8085}
          predicates:
            - Path=/ping/reward
          filters:
            - RewritePath=/ping/reward, /ping

        - id: notification-service-ping
          uri: ${NOTIFICATION_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/ping/notification
          filters:
            - RewritePath=/ping/notification, /ping

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
